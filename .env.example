# BharatYatra Environment Variables Configuration

# Secret API Key for securing external API access (Optional, defaults to internal fallback)
BHARATYATRA_API_KEY=bharatyatra_secret_key_2026

# Next.js Server Port (Optional, defaults to 3000)
PORT=3000

# Node Environment
NODE_ENV=development
